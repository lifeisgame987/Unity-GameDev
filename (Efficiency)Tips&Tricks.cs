using System.Collections;
using System.Collections.Generics;
using UnityEngine;
// Which one required import only that library

public class (Efficiency)Tips&Tricks : MonoBehaviour{
  
  // Custom Class
  
  // enums
  
  // Variables = Value
  
  void Awake(){
    // Initialise
    // Assign value to the reference
    // Singleton
  }
  
  void OnEnable(){
    // Subscribe delegates & events
  }
  
  void OnDisable(){
    // Unsubscribe delegates & events
  }
  
  void Start(){
    // Call anything that have to be done at first
  }
  
  void Update(){
    // Call anything that has to be done repeatedly
  }
  
  void FixedUpdate(){
    // Call physics based things that has to be done repeatedly
  }
  
  void LateUpdate(){
    // Call something that has to be done after Update like -> CameraFollow, ParallelX
  }
}